import { PopulateFormDirective } from './populate-form.directive';

describe('PopulateFormDirective', () => {
  it('should create an instance', () => {
    const directive = new PopulateFormDirective();
    expect(directive).toBeTruthy();
  });
});
